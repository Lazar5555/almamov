<!DOCTYPE html>
<html>
    <head>
        <title>Log in</title>
        <link type="text/css" href="../../../assets/stylesheets/stylesheet.scss" />
    </head>
    <body>
        <div class="signup" style="text-align: center; width:100%; margin: 100px 5px;">
            <h2 class="t_login">Log in</h2>
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <div class="field">
                
                <%= f.label :email, class:"attribute" %> <br />
                <%= f.email_field :email, class:"text_edit", autofocus: true %>
              </div>

              <div class="field">
                <%= f.label :password, class:"attribute" %><br />
                <%= f.password_field :password, class:"text_edit", autocomplete: "off" %>
              </div>

              <% if devise_mapping.rememberable? -%>
                <div class="field">
                  <%= f.check_box :remember_me, class:"check_box" %>
                  <%= f.label :remember_me, class:"attribute" %>
                </div>
              <% end -%>

              <div class="actions">
               <%=  f.submit "Log in", class:"botton" %>
              </div>
            <% end %>

            <%= render "devise/shared/links" %>
        </div>    
    </body>
</html>